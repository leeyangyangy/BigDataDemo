<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xyz.leeyangy.spc.mapper.PasswordHistoryMapper">

    <!-- 查询用户最近 N 条密码历史 (按创建时间倒序) -->
    <select id="selectRecentByUserId" resultType="xyz.leeyangy.spc.entity.PasswordHistory">
        SELECT id, user_id, password_hash, created_at
        FROM sys_password_history
        WHERE user_id = #{userId} AND deleted = 0
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>

    <!-- 删除用户超出保留数量之外的旧记录 (按 id 倒序保留前 keepCount 条) -->
    <delete id="deleteOldRecords">
        DELETE FROM sys_password_history
        WHERE user_id = #{userId}
          AND id NOT IN (
              SELECT id FROM (
                  SELECT id FROM sys_password_history
                  WHERE user_id = #{userId} AND deleted = 0
                  ORDER BY created_at DESC
                  LIMIT #{keepCount}
              ) AS keep_ids
          )
    </delete>

</mapper>
